fix: web & mobile polish sprint - 10 changes

Web:
- Premium loading screen: spinning dual ring, animated progress bar, brand
  name with purple glow (replaces basic emoji loader)
- WealthHistoryChart: removed 'Geçmişte Olsaydı?' what-if tab & all related
  state/effects. Cleaned up ChartMode type.
- WealthHistoryChart: replaced native <select> (white-on-white text on Win)
  with custom dark-background dropdown panel. Options now visible on Windows.
- AI bubble: now draggable via pointer events (mouse + touch). Tap to open
  chat, drag to reposition anywhere on screen. touchAction:none prevents
  scroll conflict on mobile.

Mobile:
- Bottom nav: replaced Transfer tab (converter) with Haberler (news) tab
  with newspaper SVG icon. News view already had a render case.
- Transfer/converter was not relevant to portfolio tracking use-case.
