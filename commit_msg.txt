feat(mobile): premium UI redesign matching reference images

Bottom nav: 5 tabs (Anasayfa/Portföy/Asistan/Transfer/Profil) with SVG icons.
Frosted glass dark background, purple active top-pill indicator, raised center
Asistan button with purple glow above the bar. safe-area-inset-bottom padding.

Top bar: Clean white bold Finoria title + avatar logo border, purple + button,
profile initial avatar with gradient background. Hamburger removed.

AI Chat: Replaced image avatar with glowing purple robot SVG matching
reference image 2. robotGlow animation pulses outer glow every 2s. Added
dedicated full-screen Asistan tab that renders AiPortfolioChat inline.

WealthChart: External % labels on donut segments via custom renderLabel.
Added labelLine connectors. Replaced legend with premium category rows table
(colored icon + name + % column) matching bottom table in reference image 3.
